import styled from "styled-components";

export const UserNotFoundWrapper = styled.div`
    display: flex;
    flex-direction: column;
    align-items: center;
    width: 640px; /* Can be in percentage also. */
    height: auto;
    margin: 0 auto;
    padding: 10px;
    position: relative;
`

export const UserNotFoundIcon = styled.img`
    max-width: 64px;
`

export const StyledParagraph = styled.p`
    display: flex;
    flex-direction: column;
    
    text-align: center;
    font-weight: 400;
    font-size: 22px;
    line-height: 140%;

    color: #808080;

`